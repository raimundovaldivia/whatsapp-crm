// ⚠️  DEPRECADO — Este archivo ya no se usa.
// El OAuth de Shopify ahora lo maneja la app "raigentic" (desplegada en Render).
// Ver: /A-SHOPIFY/raigentic/
module.exports = require('express').Router();
